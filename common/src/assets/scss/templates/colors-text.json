[
  <%= colors.map(function(color) {
    var obj = {};

    obj.title   = color;
    obj.class   = "u-color-"+ color;
    obj.height  = "";
    obj.width   = "";
    obj.posX    = "";
    obj.posY    = "";

    return JSON.stringify(obj);
  }) %>
]
