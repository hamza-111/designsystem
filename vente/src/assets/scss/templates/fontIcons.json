[
  <%= glyphs.map(function(glyph) {
    var obj = {};

    obj.title   = glyph.name;
    obj.class   = "icon-"+ glyph.name;
    obj.height  = "";
    obj.width   = "";
    obj.posX    = "";
    obj.posY    = "";

    return JSON.stringify(obj);
  }) %>
]
