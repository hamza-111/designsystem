//- +codePromo

h2(style="font-size:2rem;font-weight:bold;margin:10px 0 20px;") Composant codePromo

+demo({
  desc: "Pour activer le loader : injecter la classe <code>is-loading</code> à <code>c-baseBtn</code> (voir pages de démo du spinner et des boutons)"
})
  +codePromo


+demo({
  desc: "ajout bloc <code>c-codePromo__output</code>"
})
  +codePromo({
    valid: true,
    id: 'codePromo2'
  })